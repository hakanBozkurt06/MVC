@model Arac
@{
    ViewData["Title"] = "AracDuzenle";
    ViewData["Desc"] = "Araç düzenleme sayfası";
}

<h1>Araç Düzenle</h1>

<div class="form-container">
    <form asp-action="AracDuzenle" asp-controller="Arac" method="post">
        <input asp-for="AracId" type="hidden" />
        <div class="mb-3">
            <label asp-for="Marka" class="form-label"></label>
            <input asp-for="Marka" class="form-control" />
            <span asp-validation-for="Marka" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Model" class="form-label"></label>
            <input asp-for="Model" class="form-control" />
            <span asp-validation-for="Model" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Yil" class="form-label"></label>
            <input asp-for="Yil" class="form-control" />
            <span asp-validation-for="Yil" class="text-danger"></span>
        </div>
        <input type="submit" name="btnGuncelle" value="Güncelle" class="btn btn-primary" />
        <a asp-action="Index" asp-controller="Arac" class="btn btn-secondary">İptal</a>
    </form>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
